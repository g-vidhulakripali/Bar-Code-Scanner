<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the button is disabled -->
    <item android:drawable="@drawable/button_background_disabled" android:state_enabled="false" />

    <!-- Default state (when enabled) -->
    <item android:drawable="@drawable/button_background_gradient" />
</selector>